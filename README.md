# Lyceum 3D

3D facade viewer for GitHub Pages.

## GitHub Pages
Upload all files from this folder to the root of a GitHub repository, then enable:
Settings → Pages → Deploy from a branch → `main` → `/ (root)`.

The site entry point is `index.html`.
